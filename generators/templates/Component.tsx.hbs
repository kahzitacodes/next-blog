import { PropTypes } from './{{pascalCase name}}.types'
import * as S from './{{pascalCase name}}.styles'

export const {{pascalCase name}} = (props: PropTypes) => (
  <S.Wrapper {...props}>
    <h1>{{pascalCase name}}</h1>
  </S.Wrapper>
)
